const fs=require('node:fs'),path=require('node:path'),http=require('node:http'),assert=require('node:assert/strict'),{buildSync}=require('esbuild');
const {chromium}=require('C:/Users/fengluoxiao/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
(async()=>{const root=path.resolve('tmp/source-extent-ui');fs.mkdirSync(root,{recursive:true});
buildSync({stdin:{contents:`import React from 'react';import {createRoot} from 'react-dom/client';import Panel from './apps/web/src/components/SourceExtentPanel';import './apps/web/src/styles.css';window.calls=[];window.sdaDesktop={getNativeRendererStatus:async()=>({running:true}),nativeRendererSourceExtent:async v=>{window.calls.push(v);return !window.reject}};createRoot(document.getElementById('root')).render(<Panel/>);`,resolveDir:process.cwd(),loader:'tsx'},bundle:true,outfile:path.join(root,'app.js'),nodePaths:[path.resolve('apps/web/node_modules')],jsx:'automatic'});
const server=http.createServer((req,res)=>{const file=req.url==='/app.js'?'app.js':req.url==='/app.css'?'app.css':null;res.setHeader('Content-Type',file?.endsWith('js')?'text/javascript':file?'text/css':'text/html');res.end(file?fs.readFileSync(path.join(root,file)):'<html data-theme="light"><meta charset="utf-8"><link rel="stylesheet" href="/app.css"><div id="root" style="width:390px;padding:20px"></div><script src="/app.js"></script></html>')});await new Promise(r=>server.listen(0,'127.0.0.1',r));server.unref();
const browser=await chromium.launch({headless:true,channel:'msedge',args:['--mute-audio']});try{const page=await browser.newPage(),errors=[];page.on('pageerror',e=>errors.push(String(e)));await page.goto('http://127.0.0.1:'+server.address().port);
assert(!await page.getByRole('switch').isChecked());await page.getByRole('button',{name:'宽声源试听',exact:true}).click();assert(await page.getByRole('switch').isChecked());assert.deepEqual(await page.evaluate(()=>window.calls.at(-1)),{enabled:true,width:.25,diffusion:.12});
await page.reload();assert(await page.getByRole('switch').isChecked());await page.evaluate(()=>window.reject=true);await page.getByRole('switch').click();await page.getByRole('alert').waitFor();assert(await page.getByRole('switch').isChecked());
await page.evaluate(()=>window.reject=false);await page.getByRole('button',{name:'按母版',exact:true}).click();assert.deepEqual(await page.evaluate(()=>window.calls.at(-1)),{enabled:true,width:0,diffusion:0});await page.getByRole('switch').click();assert(!await page.getByRole('switch').isChecked());assert.deepEqual(errors,[]);await page.screenshot({path:path.join(root,'panel.png')});console.log('Source extent UI: native ACK, persistence, bypass, authored defaults, failed toggle preserved; no audio device.');
}finally{await browser.close();server.close();}})().catch(e=>{console.error(e);process.exitCode=1});
